import { IconButton } from "@chakra-ui/react";

// const baseStyle = {
// 	size: "lg",
// }

const defaultProps = {
	flexDirection: "row",
    minWidth: "none",
    p: "8px",
    m: "0px",
};

const MenuList = { defaultProps };
export default MenuList;
