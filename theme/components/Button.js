import { mode } from "@chakra-ui/theme-tools"

// const baseStyle = {
// 	size: "lg",
// }


const defaultProps = {
	size: "lg",
	borderRadius: "6px",
};

const Button = {defaultProps}
export default Button;