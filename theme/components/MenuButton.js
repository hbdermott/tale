import { IconButton } from "@chakra-ui/react";

// const baseStyle = {
// 	size: "lg",
// }

const defaultProps = {
	size: "md",
    as: IconButton,
    p: 2,
	borderRadius: "6px",
};

const MenuButton = { defaultProps };
export default MenuButton;
