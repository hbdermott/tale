import { mode } from "@chakra-ui/theme-tools";

const baseStyle = {
	// backgroundColor: "white",
	fontWeight: "bold",
};

const sizes = {
	xs: {
		w: "100px",
		h: "200px",
	},
	lg: {
		w: "200px",
		h: "400px",
	},
};

const Popover = { sizes };
export default Popover;
